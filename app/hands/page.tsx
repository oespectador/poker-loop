"use client";

import Link from "next/link";
import { FormEvent, useEffect, useState } from "react";
import { AppShell } from "../components/AppShell";
import { parseGgPokerCraftFile } from "@/lib/ggHandParser";
import { MAX_IMPORT_CANDIDATES, selectHandDetail, selectHandReviewCandidatePool, suggestionToRealHandInput } from "@/lib/handSuggestions";
import { clearHandSuggestions, hasProcessedImport, MAX_PENDING_HAND_SUGGESTIONS, readHandSuggestions, removeHandSuggestion } from "@/lib/handSuggestionStorage";
import { clearActiveGgImportBatch, hasRemainingImportCandidates, markImportCandidatesSurfaced, persistGgImportTransition, readActiveGgImportBatch, remainingImportCandidates, surfaceNextImportCandidates, type ActiveGgImportBatch } from "@/lib/activeGgImportBatch";
import { createRealHand, deleteRealHand, readRealHands, realHandSkillLabels, realHandStreetLabels, saveRealHand, trainingLinkForHand, updateRealHand, validateRealHandInput } from "@/lib/realHands";
import type { HandReviewSuggestion, RealHandReview, RealHandReviewInput, RealHandStreet, ReasoningFactor, Skill, StoredRealHandReasoningSnapshot } from "@/lib/types";
import { QuickReview } from "./QuickReview";
import { deleteReasoningSnapshotForHand, readReasoningSnapshots } from "@/lib/reasoningSnapshotStorage";
import { ParsedHandVisualization } from "../components/HandVisualization";
import { summarizeRealHandReviewPatterns } from "@/lib/realHandReviewPatterns";
import { deriveRealHandInvestigations } from "@/lib/realHandInvestigations";
import { reasoningFactorLabels } from "@/lib/realHandReasoning";
import { createActiveRealHandInvestigation, deriveProspectiveInvestigation, readActiveRealHandInvestigation, syncProspectiveInvestigation, writeActiveRealHandInvestigation, type ActiveRealHandInvestigation } from "@/lib/prospectiveRealHandInvestigation";
import { archiveRealHandInvestigation, completionForProspectiveResult, readRealHandInvestigationHistory, summarizeRealHandInvestigationEpisode, type StoredRealHandInvestigationEpisode } from "@/lib/realHandInvestigationHistory";
import { canExploreInvestigationInTraining, investigationTrainingLink, investigationTrainingSkillOptions } from "@/lib/investigationTrainingBridge";
import { listInvestigationTrainingLaunchesByEpisodeId, readInvestigationTrainingLaunches, type InvestigationTrainingLaunch } from "@/lib/investigationTrainingLaunches";
import { listInvestigationTrainingCompletionsForEpisode, readInvestigationTrainingCompletions, type InvestigationTrainingCompletion } from "@/lib/investigationTrainingCompletions";
import { createPostTrainingRealHandFollowUp, findActivePostTrainingRealHandFollowUp, listPostTrainingRealHandFollowUpsByEpisodeId, readPostTrainingRealHandFollowUps, summarizePostTrainingRealHandFollowUp, syncPostTrainingRealHandFollowUps, writePostTrainingRealHandFollowUps, type PostTrainingRealHandFollowUp } from "@/lib/postTrainingRealHandFollowUps";
import { deriveRealHandWindowComparisons, type RealHandWindowComparison, type WindowObservationSummary } from "@/lib/realHandWindowComparisons";
import { deriveRealHandWindowRelation, describeRealHandWindowRelation } from "@/lib/realHandWindowRelations";
import { countRemainingCandidatesByFilter, GG_EXPLORATION_FILTERS, ggExplorationFilterLabels, surfaceImportCandidatesForFilter, type GgExplorationFilter } from "@/lib/ggImportExploration";
import { chooseInitialHandsWorkspaceSection, type HandsWorkspaceSection } from "@/lib/handsWorkspace";
import { HandsWorkspaceNav } from "./HandsWorkspaceNav";

const emptyForm: RealHandReviewInput = { rawHandText: "", doubt: "", rangeRead: "", objective: "", targetsAndSizeResponse: "" };
const cardLabel = (card: string) => card.replace("h", "♥").replace("d", "♦").replace("c", "♣").replace("s", "♠");
const dateLabel = (value: string) => new Date(value).toLocaleString("pt-BR", { day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit" });
const observationText = (factor: ReasoningFactor, summary: WindowObservationSummary) => `${reasoningFactorLabels[factor]} apareceu em ${summary.factorCount} das ${summary.reviewedCount} revisões.`;
const supportText = (summary: WindowObservationSummary) => `Sustentação Baixa ou Não estava claro: ${summary.lowOrUnclearCount} de ${summary.supportRecordedCount} ocorrências com sustentação registrada.`;
async function fingerprint(text: string) { const bytes = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(text)); return [...new Uint8Array(bytes)].map((byte) => byte.toString(16).padStart(2, "0")).join(""); }

function ComparisonToggle({ comparison, open, onToggle }: { comparison: RealHandWindowComparison; open: boolean; onToggle: () => void }) {
  const relation = deriveRealHandWindowRelation(comparison);
  const description = relation ? describeRealHandWindowRelation(relation) : undefined;
  return <div className="window-comparison-toggle"><button className="text-button" type="button" aria-expanded={open} onClick={onToggle}>Comparar as duas observações</button>{open && <section className="window-comparison" aria-label="Duas janelas de observação"><div className="eyebrow">DUAS JANELAS DE OBSERVAÇÃO</div><h3>Como este fator apareceu em dois momentos</h3><p className="comparison-disclosure">Essas são duas pequenas janelas de autorrelato. Colocá-las lado a lado não permite concluir que o treino causou qualquer diferença.</p><div className="comparison-windows"><ObservationWindow title="INVESTIGAÇÃO ORIGINAL" completedAt={comparison.originalCompletedAt} factor={comparison.factor} summary={comparison.original} /><ObservationWindow title="OBSERVAÇÃO POSTERIOR" completedAt={comparison.posteriorCompletedAt} factor={comparison.factor} summary={comparison.posterior} /></div>{description && <div className="comparison-relation"><div className="eyebrow">RELAÇÃO OBSERVADA</div><strong>{description.text}</strong><p>{description.disclosure}</p></div>}<div className="comparison-session"><div className="eyebrow">SESSÃO ENTRE AS JANELAS</div><strong>{realHandSkillLabels[comparison.skill]} · foco escolhido por você.</strong></div></section>}</div>;
}

function ObservationWindow({ title, completedAt, factor, summary }: { title: string; completedAt: string; factor: ReasoningFactor; summary: WindowObservationSummary }) {
  return <article className="comparison-window"><div className="eyebrow">{title}</div><time dateTime={completedAt}>{new Date(completedAt).toLocaleDateString("pt-BR")}</time><p>{observationText(factor, summary)}</p>{factor !== "automatic" && <p>{supportText(summary)}</p>}</article>;
}

export default function HandsPage() {
  const [hands, setHands] = useState<RealHandReview[]>([]); const [suggestions, setSuggestions] = useState<HandReviewSuggestion[]>([]);
  const [selectedId, setSelectedId] = useState<string>(); const [selectedSuggestionId, setSelectedSuggestionId] = useState<string>();
  const [editingId, setEditingId] = useState<string>(); const [form, setForm] = useState<RealHandReviewInput>(emptyForm); const [message, setMessage] = useState("");
  const [importMessage, setImportMessage] = useState(""); const [activeImportBatch, setActiveImportBatch] = useState<ActiveGgImportBatch | null>(null);
  const [showImportFilters, setShowImportFilters] = useState(false); const [selectedImportFilter, setSelectedImportFilter] = useState<GgExplorationFilter>();
  const [reasoningSnapshots, setReasoningSnapshots] = useState<StoredRealHandReasoningSnapshot[]>([]);
  const [openInvestigation, setOpenInvestigation] = useState<string>();
  const [activeInvestigation, setActiveInvestigation] = useState<ActiveRealHandInvestigation | null>(null);
  const [showObservedHands, setShowObservedHands] = useState(false);
  const [history, setHistory] = useState<StoredRealHandInvestigationEpisode[]>([]);
  const [openEpisodeId, setOpenEpisodeId] = useState<string>();
  const [trainingBridge, setTrainingBridge] = useState<{ episodeId: string; skill?: Skill }>();
  const [trainingLaunches, setTrainingLaunches] = useState<InvestigationTrainingLaunch[]>([]);
  const [trainingCompletions, setTrainingCompletions] = useState<InvestigationTrainingCompletion[]>([]);
  const [followUps, setFollowUps] = useState<PostTrainingRealHandFollowUp[]>([]); const [followUpChoice, setFollowUpChoice] = useState<{ episodeId: string; sessionId?: string }>(); const [showFollowUpHands, setShowFollowUpHands] = useState(false);
  const [openComparisonId, setOpenComparisonId] = useState<string>();
  const [workspaceSection, setWorkspaceSection] = useState<HandsWorkspaceSection>("review");
  useEffect(() => {
    const snapshots = readReasoningSnapshots(); const storedInvestigation = readActiveRealHandInvestigation();
    const syncedInvestigation = storedInvestigation ? syncProspectiveInvestigation(storedInvestigation, snapshots) : null;
    if (storedInvestigation && syncedInvestigation && syncedInvestigation !== storedInvestigation) writeActiveRealHandInvestigation(syncedInvestigation, true);
    const storedFollowUps = readPostTrainingRealHandFollowUps(); const syncedFollowUps = syncPostTrainingRealHandFollowUps(storedFollowUps, snapshots); if (JSON.stringify(storedFollowUps) !== JSON.stringify(syncedFollowUps)) writePostTrainingRealHandFollowUps(syncedFollowUps);
    const storedSuggestions = readHandSuggestions(); const storedBatch = readActiveGgImportBatch();
    setHands(readRealHands()); setSuggestions(storedSuggestions); setActiveImportBatch(storedBatch); setReasoningSnapshots(snapshots); setActiveInvestigation(syncedInvestigation); setHistory(readRealHandInvestigationHistory()); setTrainingLaunches(readInvestigationTrainingLaunches()); setTrainingCompletions(readInvestigationTrainingCompletions()); setFollowUps(syncedFollowUps);
    setWorkspaceSection(chooseInitialHandsWorkspaceSection({ hasActiveInvestigation: Boolean(syncedInvestigation), hasActiveFollowUp: Boolean(findActivePostTrainingRealHandFollowUp(syncedFollowUps)), hasPendingSuggestions: storedSuggestions.length > 0, hasRemainingImportCandidates: Boolean(storedBatch && hasRemainingImportCandidates(storedBatch)) }));
  }, []);
  const reviewPatterns = summarizeRealHandReviewPatterns(reasoningSnapshots);
  const investigations = deriveRealHandInvestigations(reasoningSnapshots);
  const prospectiveResult = deriveProspectiveInvestigation(activeInvestigation, reasoningSnapshots);
  const activeFollowUp = findActivePostTrainingRealHandFollowUp(followUps); const activeFollowUpSummary = activeFollowUp ? summarizePostTrainingRealHandFollowUp(activeFollowUp) : undefined;
  const comparisons = deriveRealHandWindowComparisons(history, followUps, trainingLaunches, trainingCompletions);
  const selected = hands.find(({ id }) => id === selectedId); const selectedSuggestion = suggestions.find(({ id }) => id === selectedSuggestionId);
  function openDetail(kind: "saved" | "suggestion", id: string) { const next = selectHandDetail(kind, id); setSelectedId(next.selectedId); setSelectedSuggestionId(next.selectedSuggestionId); if (kind === "saved") setWorkspaceSection("review"); }
  function change<K extends keyof RealHandReviewInput>(key: K, value: RealHandReviewInput[K]) { setForm((current) => ({ ...current, [key]: value })); }
  async function importFile(file?: File) {
    if (!file) return; setImportMessage("");
    if (suggestions.length) return setImportMessage("Você ainda tem situações desta sessão para considerar. Salve ou descarte essas sugestões antes de importar outra sessão.");
    if (activeImportBatch && hasRemainingImportCandidates(activeImportBatch)) return setImportMessage("Continue explorando este lote ou encerre-o explicitamente antes de importar outro arquivo.");
    try {
      const text = await file.text(); const hash = await fingerprint(text);
      if (hasProcessedImport(hash)) return setImportMessage("Este arquivo já foi processado.");
      const result = parseGgPokerCraftFile(text); const candidates = selectHandReviewCandidatePool(result.hands, { limit: MAX_IMPORT_CANDIDATES }); const next = candidates.slice(0, 5);
      const batch: ActiveGgImportBatch = { version: 1, fingerprint: hash, importedAt: new Date().toISOString(), recognizedHands: result.recognizedHands, ignoredHands: result.ignoredHands, candidates, surfacedSuggestionIds: [] };
      const surfacedBatch = markImportCandidatesSurfaced(batch, next);
      persistGgImportTransition(activeImportBatch, suggestions, surfacedBatch, next, { fingerprint: hash }); setSuggestions(next); setActiveImportBatch(surfacedBatch);
      if (!result.totalBlocks) setImportMessage("Nenhuma mão GG/PokerCraft reconhecível foi encontrada neste arquivo.");
    } catch { setImportMessage("Não foi possível processar e armazenar este lote neste navegador. Seus dados existentes não foram apagados."); }
  }
  function surfaceMore(count: 5 | 10) {
    if (!activeImportBatch) return;
    try { const next = surfaceNextImportCandidates(activeImportBatch, count, suggestions); persistGgImportTransition(activeImportBatch, suggestions, next.batch, next.suggestions); setSuggestions(next.suggestions); setActiveImportBatch(next.batch); setImportMessage(next.added.length ? `${next.added.length} novas situações foram adicionadas.` : "Nenhuma outra situação pôde ser adicionada agora."); }
    catch { setImportMessage("Não foi possível armazenar as novas situações. Seus dados existentes não foram apagados."); }
  }
  function surfaceFiltered() {
    if (!activeImportBatch || !selectedImportFilter) return;
    try { const next = surfaceImportCandidatesForFilter(activeImportBatch, selectedImportFilter, 5, suggestions); persistGgImportTransition(activeImportBatch, suggestions, next.batch, next.suggestions); setSuggestions(next.suggestions); setActiveImportBatch(next.batch); setImportMessage(next.added.length ? `${next.added.length} situações deste tipo foram adicionadas.` : "Nenhuma situação deste tipo pôde ser adicionada agora."); }
    catch { setImportMessage("Não foi possível armazenar as novas situações. Seus dados existentes não foram apagados."); }
  }
  function endImportBatch() {
    if (suggestions.length) return setImportMessage("Salve ou descarte as situações pendentes antes de encerrar este lote.");
    try { clearActiveGgImportBatch(); setActiveImportBatch(null); setImportMessage("Lote encerrado. As mãos salvas e o histórico do arquivo foram mantidos."); }
    catch { setImportMessage("Não foi possível encerrar este lote no armazenamento deste navegador."); }
  }
  function discard(item: HandReviewSuggestion) { removeHandSuggestion(item.id); setSuggestions(readHandSuggestions()); setSelectedSuggestionId(undefined); }
  function discardAll() { if (!window.confirm("Descartar todas as sugestões desta sessão?")) return; clearHandSuggestions(); setSuggestions([]); setSelectedSuggestionId(undefined); }
  function promote(item: HandReviewSuggestion) {
    try {
      const saved = saveRealHand(createRealHand(suggestionToRealHandInput(item))); removeHandSuggestion(item.id);
      setHands(readRealHands()); setSuggestions(readHandSuggestions()); openDetail("saved", saved.id); setMessage("Mão salva para revisão. Complete sua reflexão quando quiser.");
    } catch { setImportMessage("Não foi possível salvar esta mão no armazenamento deste navegador."); }
  }
  function submit(event: FormEvent) { event.preventDefault(); const validation = validateRealHandInput(form); if (validation) return setMessage(validation); try { const saved = editingId ? updateRealHand(editingId, form) : saveRealHand(createRealHand(form)); if (!saved) return setMessage("Esta mão não está mais disponível."); setHands(readRealHands()); openDetail("saved", saved.id); setEditingId(undefined); setForm(emptyForm); setMessage("Mão salva."); } catch { setMessage("Não foi possível salvar a mão no armazenamento deste navegador. Seus dados existentes não foram apagados."); } }
  function edit(hand: RealHandReview) { const { id: _id, createdAt: _createdAt, ...input } = hand; setEditingId(hand.id); setForm(input); setSelectedId(undefined); setSelectedSuggestionId(undefined); setMessage(""); }
  function remove(hand: RealHandReview) { if (!window.confirm("Excluir esta mão registrada?")) return; deleteRealHand(hand.id); deleteReasoningSnapshotForHand(hand.id); setHands(readRealHands()); setReasoningSnapshots(readReasoningSnapshots()); setSelectedId(undefined); setEditingId(undefined); setMessage("Mão excluída. Seu progresso de treino não foi alterado."); }
  function follow(candidate: (typeof investigations)[number]) {
    if (activeFollowUp) return setMessage("Conclua o acompanhamento atual antes de iniciar uma nova observação.");
    if (activeInvestigation && activeInvestigation.factor !== candidate.factor && !window.confirm(`Você já está acompanhando ${reasoningFactorLabels[activeInvestigation.factor]}. Encerrar esse acompanhamento e começar ${candidate.factorLabel}?`)) return;
    if (activeInvestigation?.factor === candidate.factor) return;
    if (activeInvestigation && prospectiveResult) setHistory(archiveRealHandInvestigation(activeInvestigation, completionForProspectiveResult(prospectiveResult)));
    const next = createActiveRealHandInvestigation(candidate);
    writeActiveRealHandInvestigation(next); setActiveInvestigation(next); setShowObservedHands(false);
  }
  function endFollowing() {
    if (!activeInvestigation || !prospectiveResult) return;
    setHistory(archiveRealHandInvestigation(activeInvestigation, completionForProspectiveResult(prospectiveResult))); setActiveInvestigation(null); setShowObservedHands(false);
  }
  function refreshSnapshots() {
    const snapshots = readReasoningSnapshots(); setReasoningSnapshots(snapshots);
    const syncedFollowUps = syncPostTrainingRealHandFollowUps(followUps, snapshots); if (JSON.stringify(syncedFollowUps) !== JSON.stringify(followUps)) { writePostTrainingRealHandFollowUps(syncedFollowUps); setFollowUps(syncedFollowUps); }
    if (!activeInvestigation) return;
    const synced = syncProspectiveInvestigation(activeInvestigation, snapshots);
    if (synced !== activeInvestigation) { writeActiveRealHandInvestigation(synced, true); setActiveInvestigation(synced); }
  }
  function startPostTrainingFollowUp(episodeId: string, sessionId: string) {
    const next = createPostTrainingRealHandFollowUp({ episodeId, sessionId, episodes: history, launches: trainingLaunches, completions: trainingCompletions, snapshots: reasoningSnapshots, existingFollowUps: followUps, activeInvestigation });
    if (!next) return setMessage("Conclua o acompanhamento atual antes de iniciar uma nova observação.");
    const updated = [...followUps, next]; if (writePostTrainingRealHandFollowUps(updated)) { setFollowUps(updated); setFollowUpChoice(undefined); setShowFollowUpHands(false); }
  }
  return <AppShell>
    <section className="page-heading"><div className="eyebrow">MÃOS REAIS</div><h1>Do volume à próxima situação para considerar.</h1><p className="lead">Importações e mãos salvas são contexto para você. Elas não viram diagnóstico nem alteram seu progresso.</p></section>

    <HandsWorkspaceNav active={workspaceSection} onChange={setWorkspaceSection} pendingCount={suggestions.length} activeTracking={Boolean(activeInvestigation || activeFollowUp)} />

    {workspaceSection === "track" && <main id="hands-panel-track" className="hands-workspace" role="tabpanel" aria-labelledby="hands-tab-track" tabIndex={0}>
    {!reasoningSnapshots.length && !activeInvestigation && !activeFollowUp && !history.length && <p className="workspace-empty">Conforme você revisa mãos, padrões e acompanhamentos aparecem aqui.</p>}

    <section className="panel review-patterns" aria-labelledby="review-patterns-title"><div className="eyebrow">SUAS REVISÕES</div><h2 id="review-patterns-title">O que tem aparecido nas suas decisões</h2><p>Isso resume apenas o que você marcou durante as revisões. Não é uma avaliação de certo ou errado.</p><strong className="reviewed-count">{reviewPatterns.reviewedHands} {reviewPatterns.reviewedHands === 1 ? "decisão revisada" : "decisões revisadas"}</strong>{reviewPatterns.observations.length > 0 ? <ul className="review-observations">{reviewPatterns.observations.map((observation) => <li key={`${observation.kind}:${observation.text}`}>{observation.text}</li>)}</ul> : <p className="review-patterns-empty">{reviewPatterns.hasEnoughReviewsForObservations ? "Entre as revisões registradas, nenhum fator apareceu em pelo menos três decisões." : "Continue revisando mãos. Ainda há poucas revisões para destacar algo que tenha aparecido repetidamente."}</p>}</section>

    <section className="panel investigations" aria-labelledby="investigations-title"><div className="eyebrow">PARA INVESTIGAR</div><h2 id="investigations-title">Pontos que apareceram em várias revisões</h2><p>Essas são hipóteses baseadas no que você mesmo marcou nas suas mãos. Elas não indicam que uma decisão estava certa ou errada.</p>{investigations.length ? <div className="investigation-list">{investigations.map((candidate) => {
      const relatedHands = candidate.handReviewIds.map((id) => hands.find((hand) => hand.id === id)).filter((hand): hand is RealHandReview => Boolean(hand)).slice(0, 3);
      const isOpen = openInvestigation === candidate.factor;
      return <article className="investigation-card" key={candidate.factor}><div className="eyebrow">{candidate.factorLabel.toUpperCase()}</div><p>{candidate.text}</p><div className="investigation-actions"><button className="primary-cta compact" type="button" disabled={activeInvestigation?.factor === candidate.factor || Boolean(activeFollowUp)} onClick={() => follow(candidate)}>{activeInvestigation?.factor === candidate.factor ? "Acompanhamento ativo" : "Acompanhar nas próximas mãos"}</button>{relatedHands.length > 0 && <><button className="text-button" type="button" aria-expanded={isOpen} onClick={() => setOpenInvestigation(isOpen ? undefined : candidate.factor)}>Ver mãos relacionadas</button>{isOpen && <div className="related-hands">{relatedHands.map((hand) => <button type="button" className="hand-list-item" key={hand.id} onClick={() => openDetail("saved", hand.id)}><strong>{hand.title || "Mão registrada"}</strong><span>{new Date(hand.createdAt).toLocaleDateString("pt-BR")}{hand.street ? ` · ${realHandStreetLabels[hand.street]}` : ""}</span></button>)}</div>}</>}</div></article>;
    })}</div> : <p className="investigations-empty">Ainda não apareceu nenhum ponto com evidência suficiente no seu autorrelato para destacar como hipótese de investigação.</p>}</section>

    {activeInvestigation && prospectiveResult && <section className="panel active-investigation" aria-labelledby="active-investigation-title"><div className="eyebrow">ACOMPANHAMENTO ATIVO</div><h2 id="active-investigation-title">{reasoningFactorLabels[activeInvestigation.factor]}</h2><p>Vamos observar apenas as próximas decisões que você revisar. As mãos que criaram esta hipótese não entram novamente na contagem.</p><strong className="investigation-progress">{prospectiveResult.reviewedCount} de 5 novas decisões</strong><p>{prospectiveResult.text}</p><div className="investigation-actions">{prospectiveResult.observedHandReviewIds.some((id) => hands.some((hand) => hand.id === id)) && <button className="text-button" type="button" aria-expanded={showObservedHands} onClick={() => setShowObservedHands(!showObservedHands)}>Ver novas mãos observadas</button>}<button className={prospectiveResult.reviewedCount === 5 && prospectiveResult.status !== "inconclusive" ? "primary-cta compact" : "text-button danger-text"} type="button" onClick={endFollowing}>{prospectiveResult.reviewedCount === 5 && prospectiveResult.status !== "inconclusive" ? "Concluir acompanhamento" : "Encerrar acompanhamento"}</button></div>{showObservedHands && <div className="related-hands">{prospectiveResult.observedHandReviewIds.map((id) => hands.find((hand) => hand.id === id)).filter((hand): hand is RealHandReview => Boolean(hand)).map((hand) => <button type="button" className="hand-list-item" key={hand.id} onClick={() => openDetail("saved", hand.id)}><strong>{hand.title || "Mão registrada"}</strong><span>{new Date(hand.createdAt).toLocaleDateString("pt-BR")}{hand.street ? ` · ${realHandStreetLabels[hand.street]}` : ""}</span></button>)}</div>}</section>}

    {activeFollowUp && activeFollowUpSummary && <section className="panel active-investigation" aria-labelledby="post-training-follow-up-title"><div className="eyebrow">ACOMPANHAMENTO PÓS-TREINO</div><h2 id="post-training-follow-up-title">Observando novamente: {reasoningFactorLabels[activeFollowUp.factor]}</h2><p>Estamos observando o mesmo fator da investigação original nas próximas revisões. Isso apenas coleta fatos desta nova janela e não avalia o resultado do treino.</p><strong className="investigation-progress">{activeFollowUpSummary.reviewedCount} de 5 novas revisões</strong><p>{activeFollowUpSummary.text}</p>{activeFollowUp.observations.some(({ handReviewId }) => hands.some(({ id }) => id === handReviewId)) && <button className="text-button" type="button" aria-expanded={showFollowUpHands} onClick={() => setShowFollowUpHands(!showFollowUpHands)}>Ver mãos observadas</button>}{showFollowUpHands && <div className="related-hands">{activeFollowUp.observations.map(({ handReviewId }) => hands.find(({ id }) => id === handReviewId)).filter((hand): hand is RealHandReview => Boolean(hand)).map((hand) => <button type="button" className="hand-list-item" key={hand.id} onClick={() => openDetail("saved", hand.id)}><strong>{hand.title || "Mão registrada"}</strong></button>)}</div>}</section>}

    <section className="panel investigation-history" aria-labelledby="investigation-history-title"><div className="eyebrow">HISTÓRICO DE INVESTIGAÇÕES</div><h2 id="investigation-history-title">O que você já acompanhou</h2>{history.length ? <div className="investigation-list">{history.map((episode) => {
      const summary = summarizeRealHandInvestigationEpisode(episode); const availableHands = episode.prospectiveReviews.map(({ handReviewId }) => hands.find(({ id }) => id === handReviewId)).filter((hand): hand is RealHandReview => Boolean(hand)); const isOpen = openEpisodeId === episode.id; const bridgeIsOpen = trainingBridge?.episodeId === episode.id; const trainingHref = bridgeIsOpen ? investigationTrainingLink(episode.id, trainingBridge.skill) : undefined; const launchCount = listInvestigationTrainingLaunchesByEpisodeId(trainingLaunches, episode.id).length; const completedSessions = listInvestigationTrainingCompletionsForEpisode(trainingCompletions, trainingLaunches, episode.id); const completionCount = completedSessions.length; const episodeFollowUps = listPostTrainingRealHandFollowUpsByEpisodeId(followUps, episode.id); const episodeComparisons = comparisons.filter((comparison) => comparison.episodeId === episode.id); const choiceOpen = followUpChoice?.episodeId === episode.id;
      const stateLabel = episode.completion === "completed" ? "Concluído" : episode.completion === "stopped" ? "Encerrado antes do fim" : "Inconclusivo";
      return <article className="investigation-card" key={episode.id}><div className="eyebrow">{reasoningFactorLabels[episode.factor].toUpperCase()}</div><strong>{stateLabel} · {new Date(episode.endedAt).toLocaleDateString("pt-BR")}</strong><p>{summary.text}</p>{launchCount > 0 && <p className="optional-note">{launchCount} {launchCount === 1 ? "sessão iniciada" : "sessões iniciadas"} · {completionCount} {completionCount === 1 ? "concluída" : "concluídas"}.</p>}{episodeFollowUps.filter(({ windowCompletedAt }) => windowCompletedAt).length > 0 && <p className="optional-note">{episodeFollowUps.filter(({ windowCompletedAt }) => windowCompletedAt).length} {episodeFollowUps.filter(({ windowCompletedAt }) => windowCompletedAt).length === 1 ? "acompanhamento pós-treino concluído" : "acompanhamentos pós-treino concluídos"}.</p>}<div className="investigation-actions">{availableHands.length > 0 && <><button className="text-button" type="button" aria-expanded={isOpen} onClick={() => setOpenEpisodeId(isOpen ? undefined : episode.id)}>Ver mãos observadas</button>{isOpen && <div className="related-hands">{availableHands.map((hand) => <button type="button" className="hand-list-item" key={hand.id} onClick={() => openDetail("saved", hand.id)}><strong>{hand.title || "Mão registrada"}</strong><span>{new Date(hand.createdAt).toLocaleDateString("pt-BR")}{hand.street ? ` · ${realHandStreetLabels[hand.street]}` : ""}</span></button>)}</div>}</>}{episodeComparisons.map((comparison) => <ComparisonToggle key={comparison.followUpId} comparison={comparison} open={openComparisonId === comparison.followUpId} onToggle={() => setOpenComparisonId(openComparisonId === comparison.followUpId ? undefined : comparison.followUpId)} />)}{completionCount > 0 && <button className="text-button" type="button" aria-expanded={choiceOpen} disabled={Boolean(activeFollowUp || activeInvestigation)} onClick={() => setFollowUpChoice(choiceOpen ? undefined : { episodeId: episode.id })}>Observar novamente nas próximas mãos</button>}{canExploreInvestigationInTraining(episode.completion) && <button className="text-button" type="button" aria-expanded={bridgeIsOpen} onClick={() => setTrainingBridge(bridgeIsOpen ? undefined : { episodeId: episode.id })}>Explorar em treino</button>}</div>{choiceOpen && <div className="training-bridge"><h3>Escolha a sessão concluída</h3><div className="training-skill-options">{completedSessions.map((completion) => { const launch = trainingLaunches.find(({ sessionId }) => sessionId === completion.sessionId)!; return <button className={`training-skill-option ${followUpChoice.sessionId === completion.sessionId ? "selected" : ""}`} type="button" key={completion.sessionId} onClick={() => setFollowUpChoice({ episodeId: episode.id, sessionId: completion.sessionId })}>{realHandSkillLabels[launch.skill]} · concluída em {new Date(completion.completedAt).toLocaleDateString("pt-BR")}</button>; })}</div>{followUpChoice.sessionId && <button className="primary-cta compact" type="button" onClick={() => startPostTrainingFollowUp(episode.id, followUpChoice.sessionId!)}>Começar acompanhamento</button>}</div>}{bridgeIsOpen && <div className="training-bridge"><h3>Escolha o que você quer explorar</h3><p>Essas mãos não determinam automaticamente o que precisa ser treinado. Escolha o foco que você considera mais útil para explorar agora.</p><p className="optional-note">Você escolhe o foco que considera mais útil para explorar. O Poker Loop não inferiu automaticamente uma habilidade a partir dessas mãos. Essa escolha é sua.</p><div className="training-skill-options">{investigationTrainingSkillOptions.map(({ skill, label }) => <button className={`training-skill-option ${trainingBridge.skill === skill ? "selected" : ""}`} type="button" aria-pressed={trainingBridge.skill === skill} key={skill} onClick={() => setTrainingBridge({ episodeId: episode.id, skill })}>{label}</button>)}</div>{trainingHref && <Link className="primary-cta compact" href={trainingHref}>Iniciar treino com este foco</Link>}</div>}</article>;
    })}</div> : <p>Nenhum acompanhamento encerrado ainda.</p>}</section>

    </main>}

    {workspaceSection === "explore" && <main id="hands-panel-explore" className="hands-workspace" role="tabpanel" aria-labelledby="hands-tab-explore" tabIndex={0}>
    {!activeImportBatch && !suggestions.length && <p className="workspace-empty">Importe uma sessão GG/PokerCraft para separar situações que valem uma segunda olhada.</p>}
    <section className="panel import-panel"><div><div className="eyebrow">IMPORTAR SESSÃO GG/POKERCRAFT</div><h2>Comece por até cinco destaques e explore mais somente se quiser.</h2><p>Seu arquivo .txt é processado localmente neste navegador e não é enviado a um servidor.</p></div><label className={`primary-cta file-cta ${suggestions.length || (activeImportBatch && hasRemainingImportCandidates(activeImportBatch)) ? "disabled" : ""}`}>Escolher arquivo<input type="file" accept=".txt,text/plain" disabled={Boolean(suggestions.length || (activeImportBatch && hasRemainingImportCandidates(activeImportBatch)))} onChange={(event) => { void importFile(event.target.files?.[0]); event.target.value = ""; }} /></label></section>
    {suggestions.length > 0 && <p className="form-message">Você ainda tem situações desta sessão para considerar. Salve ou descarte essas sugestões antes de importar outra sessão.</p>}
    {importMessage && <p className="form-message" role="status">{importMessage}</p>}
    {activeImportBatch && <section className="import-summary" aria-live="polite"><div className="eyebrow">LOTE ATIVO</div><strong>{activeImportBatch.recognizedHands} mãos reconhecidas.</strong><span>{activeImportBatch.surfacedSuggestionIds.length} situações mostradas até agora.</span><span>{remainingImportCandidates(activeImportBatch).length} outras situações disponíveis neste lote.{activeImportBatch.ignoredHands ? ` ${activeImportBatch.ignoredHands} blocos foram ignorados com segurança.` : ""}</span><p>Essas situações foram separadas somente pela estrutura da mão. Isso não significa que houve erro nelas.</p>
      {hasRemainingImportCandidates(activeImportBatch) && suggestions.length < MAX_PENDING_HAND_SUGGESTIONS && <div className="suggestion-actions"><button className="text-button" type="button" onClick={() => surfaceMore(5)}>+ 5 situações</button><button className="text-button" type="button" onClick={() => surfaceMore(10)}>+ 10 situações</button></div>}
      {hasRemainingImportCandidates(activeImportBatch) && <button className="text-button import-explore-toggle" type="button" aria-expanded={showImportFilters} onClick={() => setShowImportFilters((open) => !open)}>Explorar situações específicas</button>}
      {showImportFilters && hasRemainingImportCandidates(activeImportBatch) && (() => { const counts = countRemainingCandidatesByFilter(activeImportBatch); const selectedCount = selectedImportFilter ? counts[selectedImportFilter] : 0; return <div className="import-filter-panel"><h3>Que tipo de situação você quer revisar?</h3><p>Esses filtros usam apenas características observáveis da mão. Eles não indicam que houve erro.</p><div className="import-filter-grid">{GG_EXPLORATION_FILTERS.map((filter) => <button type="button" key={filter} disabled={counts[filter] === 0} className={selectedImportFilter === filter ? "selected" : ""} aria-pressed={selectedImportFilter === filter} onClick={() => setSelectedImportFilter(filter)}><strong>{ggExplorationFilterLabels[filter]}</strong><span>{counts[filter]} disponíveis</span></button>)}</div>{selectedImportFilter && selectedCount > 0 && <div className="filtered-import-action"><p>{selectedCount} {selectedCount === 1 ? "situação deste tipo ainda está disponível." : "situações deste tipo ainda estão disponíveis."}</p><button className="primary-cta compact" type="button" disabled={suggestions.length >= MAX_PENDING_HAND_SUGGESTIONS} onClick={surfaceFiltered}>Mostrar até 5</button></div>}</div>; })()}
      {hasRemainingImportCandidates(activeImportBatch) && suggestions.length >= MAX_PENDING_HAND_SUGGESTIONS && <p>Salve ou descarte algumas situações para continuar explorando.</p>}
      {!hasRemainingImportCandidates(activeImportBatch) && <p>Todas as situações separadas para este lote já foram mostradas.</p>}
      {!suggestions.length && hasRemainingImportCandidates(activeImportBatch) && <div className="suggestion-actions"><button className="text-button" type="button" onClick={() => setImportMessage("Use +5 ou +10 para continuar explorando este lote.")}>Continuar explorando este lote</button><button className="text-button danger-text" type="button" onClick={endImportBatch}>Encerrar lote e importar outro</button></div>}
    </section>}

    <section className="section-block"><div className="suggestion-heading"><div><div className="eyebrow">SUGESTÕES DA SESSÃO</div><p className="lead">Situações expostas voluntariamente, escolhidas somente pela estrutura da mão.</p></div>{suggestions.length > 0 && <button className="text-button danger-text" type="button" onClick={discardAll}>Descartar todas as sugestões</button>}</div>
      {!suggestions.length && <p className="lead">Nenhuma sugestão pendente.</p>}<div className="suggestion-grid">{suggestions.map((item) => <article className="panel suggestion-card" key={item.id}><div className="hero-cards">{item.heroCards.map(cardLabel).join(" ")}</div><span>{dateLabel(item.playedAt)}</span><div className="eyebrow">{item.reasonLabel}</div><p>{item.reasonMessage}</p><div className="suggestion-actions"><button className="text-button" onClick={() => openDetail("suggestion", item.id)}>Ver mão</button><button className="text-button danger-text" onClick={() => discard(item)}>Descartar</button><button className="primary-cta compact" onClick={() => promote(item)}>Salvar para revisão</button></div></article>)}</div>
    </section>

    {selectedSuggestion && <article className="panel hand-detail"><div className="eyebrow">MÃO JOGADA</div><h2>{selectedSuggestion.heroCards.map(cardLabel).join(" ")} · {dateLabel(selectedSuggestion.playedAt)}</h2><ParsedHandVisualization rawHandText={selectedSuggestion.rawHandText}/><details className="raw-history"><summary>Ver histórico bruto</summary><pre className="raw-hand-text">{selectedSuggestion.rawHandText}</pre></details><div className="eyebrow reflection-heading">POR QUE ELA APARECEU AQUI?</div><h2>{selectedSuggestion.reasonLabel}</h2><p>{selectedSuggestion.reasonMessage}</p><p>Essa seleção considera apenas a estrutura da mão. Ela não indica que sua decisão foi correta ou incorreta.</p><div className="suggestion-actions"><button className="text-button danger-text" onClick={() => discard(selectedSuggestion)}>Descartar</button><button className="primary-cta compact" onClick={() => promote(selectedSuggestion)}>Salvar para revisão</button></div></article>}

    </main>}

    {workspaceSection === "review" && <main id="hands-panel-review" className="hands-workspace" role="tabpanel" aria-labelledby="hands-tab-review" tabIndex={0}>
    <section className="section-block"><div className="eyebrow">PARA REVISAR</div>{!hands.length && <p className="lead">As mãos que você salvar para revisão aparecerão aqui.</p>}<div className="hand-list">{hands.map((hand) => <button type="button" className="hand-list-item" key={hand.id} onClick={() => openDetail("saved", hand.id)}><strong>{hand.title || "Mão registrada"}</strong><span>{new Date(hand.createdAt).toLocaleDateString("pt-BR")}{hand.street ? ` · ${realHandStreetLabels[hand.street]}` : ""}</span>{hand.trainingFocus && <span>{realHandSkillLabels[hand.trainingFocus]}</span>}</button>)}</div></section>
    {selected && <article className="panel hand-detail"><div className="hand-detail-actions"><button className="text-button" type="button" onClick={() => edit(selected)}>Editar</button><button className="text-button danger-text" type="button" onClick={() => remove(selected)}>Excluir</button></div><div className="eyebrow">MÃO JOGADA</div><h2>{selected.title || "Mão registrada"}</h2><QuickReview key={`${selected.id}:${selected.rawHandText}`} hand={selected} onSnapshotSaved={refreshSnapshots}/><details className="raw-history"><summary>Ver histórico bruto</summary><pre className="raw-hand-text">{selected.rawHandText}</pre></details><details className="detailed-reflection"><summary>Adicionar ou ver reflexão detalhada</summary><div className="eyebrow reflection-heading">SUA REFLEXÃO</div><dl className="reflection-list"><div><dt>Onde ficou sua principal dúvida?</dt><dd>{selected.doubt || "Não informado."}</dd></div><div><dt>O que você acreditava sobre o range do Vilão naquele momento?</dt><dd>{selected.rangeRead || "Não informado."}</dd></div><div><dt>Qual era o seu objetivo com a ação?</dt><dd>{selected.objective || "Não informado."}</dd></div><div><dt>Quais mãos você estava tentando atingir e elas mudariam de decisão se o tamanho aumentasse?</dt><dd>{selected.targetsAndSizeResponse || "Não informado."}</dd></div></dl>{selected.trainingFocus ? <><p>Foco escolhido por você: <strong>{realHandSkillLabels[selected.trainingFocus]}</strong></p><Link className="primary-cta compact" href={trainingLinkForHand(selected)!}>Treinar este tema</Link></> : <p>Você pode voltar a esta mão depois e escolher um foco de treino.</p>}</details></article>}

    <details className="panel manual-entry section-block" open={Boolean(editingId)}><summary><span className="eyebrow">ADICIONAR MANUALMENTE</span><strong>{editingId ? "Editar reflexão" : "Registrar uma mão sem importar uma sessão"}</strong></summary><form onSubmit={submit} className="hand-form">
      <label>Histórico da mão <textarea required rows={10} value={form.rawHandText} onChange={(e) => change("rawHandText", e.target.value)} placeholder="Cole aqui o histórico bruto da mão." /></label><label>Título <input value={form.title ?? ""} onChange={(e) => change("title", e.target.value)} placeholder="BTN vs BB — dúvida no river" /></label><label>Onde ficou a principal dúvida? <select value={form.street ?? ""} onChange={(e) => change("street", (e.target.value || undefined) as RealHandStreet | undefined)}><option value="">Não informar</option>{Object.entries(realHandStreetLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Descreva sua principal dúvida <textarea rows={3} value={form.doubt} onChange={(e) => change("doubt", e.target.value)} /></label><label>O que você acreditava sobre o range do Vilão naquele momento? <textarea rows={3} value={form.rangeRead} onChange={(e) => change("rangeRead", e.target.value)} /></label><label>Qual era o seu objetivo com a ação? <textarea rows={3} value={form.objective} onChange={(e) => change("objective", e.target.value)} /></label><label>Quais mãos você estava tentando atingir e elas mudariam de decisão se o tamanho aumentasse? <textarea rows={3} value={form.targetsAndSizeResponse} onChange={(e) => change("targetsAndSizeResponse", e.target.value)} /></label><label>O que você gostaria de treinar a partir desta mão? <small>Esta é uma escolha sua, não um diagnóstico do Poker Loop.</small><select value={form.trainingFocus ?? ""} onChange={(e) => change("trainingFocus", (e.target.value || undefined) as Skill | undefined)}><option value="">Ainda não sei</option>{Object.entries(realHandSkillLabels).map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label>{message && <p className="form-message" role="status">{message}</p>}<button className="primary-cta" type="submit">{editingId ? "Salvar alterações" : "Salvar mão"}</button>{editingId && <button type="button" className="text-button" onClick={() => { setEditingId(undefined); setForm(emptyForm); }}>Cancelar edição</button>}
    </form></details>
    </main>}
  </AppShell>;
}
